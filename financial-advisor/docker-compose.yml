services:
  db:
    image: postgres:16
    container_name: financial-advisor-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-advisor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-advisor_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-financial_advisor}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-advisor}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata: